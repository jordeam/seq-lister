extends base

block content
  h1 Lista de Fabricantes

  p
    form(method="post" action="/manufacturer/create" onsubmit="return insert_with_msg('Nome do fornecedor:', this.name);")
      input(type="hidden" name="name" value="")
      button.btn.btn-primary(type="submit")
        img(src='/image/add-icon.svg' alt="Adicionar" width="20pt" height="24pt")

  dl
  each val in manufacturers
    dd
      a.btn.btn-primary(href='/manufacturer/' + val.id.toString())
        img(src="/image/edit-icon.svg" alt="Editar")
      | #{val.name}

  else
    li Não existem fornecedores.

  script(src="/script/component_new.js")
